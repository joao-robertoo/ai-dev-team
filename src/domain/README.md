# Domain Layer (`src/domain`)

Contém as regras de negócio puras, entidades do sistema e interfaces principais do SaaS.

- `entities/`: Modelos de dados primários (ex: Projeto, Tarefa, Agente, Sessão).
- `interfaces/`: Contratos de serviços, provedores e repositórios.
