# Agent Tools (`src/agents/tools`)

Declaração e implementação de ferramentas (Function Calling) que os agentes podem invocar para interagir com o ambiente (ex: leitor de arquivos, manipulador de repositório, executor de linter).
