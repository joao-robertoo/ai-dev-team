# Tests (`tests`)

Suíte de testes da aplicação.

- `unit/`: Testes unitários de serviços, utilitários e regras de domínio.
- `integration/`: Testes de integração de endpoints da API e fluxos de orquestração.
